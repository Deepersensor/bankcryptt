import 'package:flutter/material.dart';

class NearFieldPayPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('NearField Pay'),
      ),
      body: Center(
        child: Text('Welcome to NearField Pay'),
      ),
    );
  }
}
